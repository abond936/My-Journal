Frequently the AI assistant receives messages that its edits are not accepted or otherwise incorrect.
This is often not the case, as the code appears as intended in the editor.
When the AI assistant receives a message that its edits are not accepted, it should immediately
directly check the code instead of relying on the message.