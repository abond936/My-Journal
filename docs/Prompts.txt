Prompts

1) Session Start (grounding only)
Read `AGENTS.md`, `.cursor/rules/# AI_InteractionRules.mdc`, and `docs/01-Vision-Architecture.md` (TECHNICAL backend/frontend principles). Then read the relevant feature area in `docs/02-Application.md` and sequencing context in `docs/03-Implementation.md` if needed. Confirm completion only; no summary.

2) Assess Request
ASSESS this request:
- Outcome:
- Constraints:
- Affected area(s):
- Risks (architecture, UI, integrity, error handling):
Return findings grounded in docs + current code behavior.

3) Plan Request
RECOMMEND a plan with max 3 substantive items:
- Option(s) + recommendation
- Exact edit scope (files/surfaces)
- Verification plan
Then ask: "Do you approve this exact edit set? May I proceed?"

4) Execute Request
EXECUTE only approved items. Keep scope closed. Preserve product invariants. After edits, report:
- What changed
- What was verified
- Residual risks / follow-ups

5) Doc Update Request
Update docs as part of done state where behavior or architecture changed:
- `docs/02-Application.md` for feature/status truth
- `docs/03-Implementation.md` for active `⭕1` sequencing
- `docs/01-Vision-Architecture.md` for architectural facts/decisions
Keep entries concise and structured (title + one-line description), not long prose.




