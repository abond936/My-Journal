# CURRENT CONTINUITY

This file is a concise operational handoff for the next project session. It is not product canon and must not override `01`, `02`, or `03`. Replace its contents at the next deliberate session handoff rather than accumulating history.

## Canon checkpoint

- `01` remains the owner of vision, architecture, technical principles, and stable invariants. Structural boundaries and operating closeout are now verified architecture rather than Planned constraints.
- `02` records current product truth, including completed Reader navigation and presentation, authoring alignment, account-backed Light/Dark choice, governed Theme standardization, administration feedback, service/component boundaries, and operational recovery.
- `03` records verified implementation state and remaining work. Parts 1–3 passed their defined Completion Gates. The bounded Media/Tagging authoring baseline and State Communication stabilization are verified; broader first-user usability, identity merge/split, face assistance, and import-source expansion remain deferred. Release Candidate is the only active gate.

## Delivered and verified

- The tag and identity system supports stable human and non-human identities, nested names, optional family-tree facts, focused dimensional assignment, multiple subjects, and author-controlled migrations. Stored compatibility profile and perspective data remain, but perspective-relative relationship labels are no longer the target information architecture.
- Gallery-to-card inheritance is complete: existing cards remain protected by per-dimension overrides; configured new-card defaults govern new cards; Unreviewed takes precedence; N/A and Unknown remain intentional assignments; subject presentation follows the agreed single, Multiple, and Subjects+ rules.
- Cross-application feedback uses localized progress, non-reflowing floating messages, shared confirmations, explicit Compose Saving/Saved/Retry state, and user-centered wording.
- Viewing users are read-only. Administrators can perform practical contextual card and visible-media editing from Reader on desktop and mobile, with Studio retaining archive-wide and bulk work.
- Studio Cards and Media share search/filter/tag-assignment and compact density foundations where their jobs overlap.
- Studio Compose and focused Reader editing share field meaning and authored-excerpt precedence without repeating basic definitions inline. Explanatory material belongs in Help rather than the authoring forms.
- Media source-byte identity evidence exists for 3,064 of 3,503 Media records; 3,062 have uncontested identity-registry entries. The paired backup used for the evidence work is `run-2026-07-17T18-25-44-195Z` with 5,875 Firestore documents and 10,639 Storage objects.
- Studio Media now contains an author-only Exact matches queue with Unresolved, Reviewed, and All views. It can record Same asset, Keep both, or Defer. Recording a review does not merge, delete, register identities, or move references.
- The author recorded Defer for the sole exact pair. All 439 records without source-byte evidence are now explicitly assessed: 101 local originals not found and 338 source originals not retained. The live audit reports zero overlap, invalid assessments, or unclassified records.
- Complete paired backup `run-2026-07-17T20-58-32-604Z` contains 5,875 Firestore documents and 10,639 verified Storage objects and preceded the classification apply.
- New image imports durably record source, metadata, Studio rendition, Reader rendition, and search-index readiness. Only pending or failed items add Studio status chrome, and targeted retry preserves the canonical Media record and relationships.
- Cards and Media now share narrow search and empty-state recovery and `Clear selection` wording where their bank operations overlap. Media deletion first verifies authoritative linked-card consequences, discloses affected-card counts plus Cover, Gallery, and body removal, and blocks if verification fails.
- Pending story piles can be merged transactionally into a chosen destination. Destination metadata survives, membership is destination-first and deduplicated, confirmed archive truth is untouched, and the source remains as merged recovery evidence. Shared confirmations pair the themed Window surface with Administration chrome text for readable neutral decisions.

## Unresolved exact pair

- `HcdSOUeqx3YY3LeedXVa` — `chair-havertys.webp`
- `wpQVDYRH3ExuPrcX1GET` — `sofa1-harvertys.webp`
- The records have identical original bytes but different plausible names for the same composite image. They have no tags, captions, or card references. They remain deliberately unregistered and are Deferred by author decision.
- Do not enter same-asset reconciliation unless the author explicitly changes this review decision.

## Next required sequence

1. Intentionally review the accumulated worktree, create a fresh paired backup while retaining no more than the three newest complete runs, commit and push the chosen revision, and run deployment checks against that exact revision before treating it as a deployable v1 candidate.
2. Validate the deployed candidate on representative administrator and reader accounts across desktop and mobile, including authentication, Studio authoring, Reader navigation/editing, Theme persistence, media presentation, backup status, and rollback readiness.
3. Keep first-user onboarding, identity merge/split, face assistance, Quote, Video, external photo libraries, Bursts, likely-match review, discretionary Theme tuning, major platform upgrades, and CSP deferred until explicitly reactivated.

## Active vocabulary engineering gate

- Read-only production evidence found 138 Who tags and 10 Person documents. Every Person document ID already equals its linked Who tag ID, every linked tag exists, and all 20 stored aliases are already represented by nested or legacy Who tag IDs.
- The Person records contain no unique gender, notes, merge-state, or non-human profile data. There are zero relationship records and zero Group records. The saved archive perspective already uses the same ID as the corresponding Person tag.
- Decision: a selected canonical Who tag ID is the identity reference without requiring a visible Person/Name or human/non-human type. Optional profile data may be a sidecar keyed by that ID but never a second identity authority. Parent/partner relationships, perspective, migration references, and future face evidence use those Who tag IDs.
- No production data was changed by this gate. Existing Person and Group collections remain intact until the manifested compatibility migration, author approval, backup, apply audit, and rollback proof are complete.
- The first four vocabulary implementation slices are complete in their corrected hierarchy-native form. Tag validation and identity resolution are tag-first; names, variants, aggregates, assignability, and drag/reparent remain in the shared tree. The separate Studio People workflow is removed, optional gender, perspective, and parent/partner relationships are available through Who tag actions, and Reader Cards/Media use server-backed Any/All selection consistently across search, Random, Typesense, and Firestore fallback paths.
- The reproducible read-only Who manifest inspected 138 Who tags, 10 compatibility profiles, 1,281 Cards, 3,503 Media records, and 270 Questions with zero writes. All compatibility Person IDs already equal canonical Who tag IDs, archive perspective resolves, and there are no relationship or Person Group records; therefore no compatibility-ID migration, assignment rewrite, count repair, or search reindex is required. Nine approved nested-name clusters are already supported by hierarchy.
- The author-reviewed Who cleanup is complete. Karen Albright, Aileen Ferguson, Anne Frank, Betty Edwards, Ed Edwards, Craig Neimiec, Gov. Carroll Campbell, Jerry Pierson, and Stephanie Baker are beneath Friends. Bob & Sandra, Children, Cousins, Father, Grandkids, Mother, Parents, Siblings, and Spouse were removed through the integrated reconciler. Final verification scanned 1,281 Cards, 3,503 Media records, and 270 Questions and found no obsolete-ID references.
- A rejected Who drag exposed a client reconciliation defect: the server correctly rejected a cycle, but Studio retained the invalid optimistic parent and could no longer render its tree. A full reload restored the authoritative acyclic hierarchy. Studio now validates the prospective hierarchy before optimistic display and reloads authoritative tags after any rejected server move. The live tree is visible again; focused cycle tests, lint, production typecheck, and diff checks pass.
- The earlier name-based Family flattening has been corrected only for the nine people explicitly named by the author; no further classifier-driven reorganization is authorized. The guarded cleanup initially stopped before deletion when a Firestore delete sentinel reached a search projection. That defect was repaired, strict indexing completed, and authoritative deletion occurred last. The reconciler remains the required path for future hierarchy changes.
- Product direction is clarified without expanding v1: the product remains a private family story archive, not a social feed. Future personal workspaces may explicitly publish selected Cards and only their referenced Media into authorized family spaces. A chronological family feed is one view of that durable shared archive. Stable Who identities and optional parent/partner facts may support a family-tree view; relationship-relative labels do not rewrite authored titles, perspective, assignments, or the primary tag hierarchy.
- The governed Tag mutation and Card completeness foundation is complete without production mutation during verification. Rename, same-dimension reparent, Remove Everywhere, and Merge share the reconciler. Remove Everywhere previews affected Cards, Media, Questions, child promotion, and newly Incomplete objects; Merge adds a same-dimension target selector, replaces source assignments and subjects with the target, and moves direct children beneath it. Self, cross-dimension, and descendant-target merges are blocked. Both destructive paths require explicit confirmation, reconcile projections/completeness/counts, strictly refresh Card and Media search, and delete the authoritative source tag last. Unqualified deletion and silent subtree deletion are blocked; retry refreshes the full Card/Media indexes before deletion so a prior projection write cannot hide the affected set. Focused mutation and Tag UI tests pass 18 cases; touched lint and production typecheck pass. Live browser verification reached both removal and merge impact confirmation and cancelled; no production data was mutated and no browser errors were logged. The next implementation slice should be selected from the next sequenced item in `03`, not further Tag mutation work.

## Verification checkpoint

- Current stabilization gates: full lint passes; production typecheck passes; all 165 active Jest suites and 891 tests pass, with 3 emulator suites and 23 tests skipped by their declared environment gate. The diagnostic in-band run exits normally. Live Reader, Studio Media, and Tag hierarchy checks pass on a clean development server. After clearing the competing development output and recovering disk space from an abnormal 3.92 GB Edge extension-activity database, the clean production build passed compilation, type validation, all 52 static pages, optimization, and route tracing.
- Fresh complete paired release backup `run-2026-07-22T13-56-38-736Z` contains 8,932 Firestore documents and 10,639 verified Storage objects. Backup retention is now three complete runs maximum; successful Windows runs request OneDrive Files On-Demand `Free up space`, and interrupted runs can resume after revalidating existing local objects. The incomplete July 21 Firestore-only run was removed, leaving the fresh run plus `run-2026-07-17T20-58-32-604Z` as the two retained complete paired backups.

- Exact-match service validation requires two existing Media records with identical SHA-256 source-byte evidence before accepting a decision.
- The Compose slice has 35 passing focused CardForm, Reader quick-edit, and patch-reconciliation tests. Touched-code lint, production typecheck, diff checks, and the production build pass.
- Compose is verified complete with no inline definitions; Help owns any future field explanation.
- Media readiness derivation, retry, grid, route, and admin-boundary checks pass 150 focused tests; production typecheck and build pass.
- Read-only live audit: 3,503 Media records, zero assessed, 3,503 legacy-unassessed, and no writes. The production server is healthy and rejects anonymous readiness-audit access with 403.
- Temporary production verification passed healthy, controlled-failure, and targeted-retry states on one Media ID with protected fields unchanged. Cleanup removed the document and all three Storage objects; the final audit confirmed zero temporary records and 3,503 total Media records.
- Browser verification confirmed the three queue views, empty Reviewed state, safe detail opening, canonical selection enabling Same asset, and return to normal Browse mode. No review decision was submitted during verification.
- Live Studio verification confirmed Media query-named no-results recovery, both clear controls, and that clearing search preserves the selected Local source filter. Filters were restored and no deletion was invoked. Twenty-two focused interaction-consistency tests, production typecheck, and production build pass; touched-code lint has no errors.
- Pile merge passes 155 focused merge-contract, Media, and administrator-route tests plus three shared-dialog behavior tests and the production build. The author verified a successful merge. The reported confirmation contrast defect was corrected through existing Window, chrome-text, and control theme roles; the rebuilt server on port 3003 serves the fix.
- Left Navigation removes Reader Group after diagnosis proved it grouped only the loaded 20-card page and could present incomplete structure as archive-wide. Legacy persisted Group state is cleared. More Controls now orders Search, Subtags and Match, then archive-wide server Sort in Random, Who, What, When, Where, Title, and Created order. Plain Who, What, and Where sorts now use Typesense ordering followed by authoritative Firestore hydration instead of missing Firestore composite indexes; route tests and read-only live projection checks cover all three. Any future grouping requires archive-wide inventory and per-group pagination. The author verified the result and the Completion Gate passed.
- Content Page inventory distinguishes Content Grid, Explore More, Read More, and Compose preview by context. Explore More now presents all five published types as compact linked destinations regardless of Content Grid presentation, restores a readable four-slot chip row, and preserves detail return context without rewriting Inline or Static behavior.
- Compose Reader preview now uses the actual noninteractive Reader renderer for all seven valid type/presentation combinations, remains visible without a cover, and preserves live focal positioning. Thirty-three focused tests, lint, production typecheck, and the production build pass; the author verified the result.
- Read More now represents all five authored child types as medium linked destinations while preserving order, chips, parent return context, and stored Content Grid presentation. Nineteen focused tests, lint, and production typecheck pass; the author verified the result.
- Quote remains unfinished but is parked by author decision. A read-only inventory found two draft and zero published Quote cards, so it does not block the higher-value Content Page sequence.
- Question Open and Reveal is verified without a data-model migration. Open retains prompt-led detail navigation. Reveal uses a reversible dissolve between a chip-bearing question face and a bounded RTE answer face whose text, image, and caption are centered as one content group; it supports one embedded image/caption and card mentions, and is blocked in Compose when empty, multi-image, or overflowing. Covered Questions retain a visible question cue. No-cover grid, Reveal, Compose preview, detail, Explore More, and Read More faces now share one container-relative watermark and prompt scale with length-aware fitting. The live 331-pixel Reader tile measures a 257.9-pixel watermark, approximately 15% above the former 224-pixel maximum; rail and open contexts scale from the same ratio. Twenty-five focused tests, lint, production typecheck, and desktop/narrow browser checks pass.
- First author verification confirmed answer reveal and restore but found that the closed Reveal face did not match the standard no-cover Open Question tile. The generic Inline exclusion from the square feed-shell classifier was corrected first, then a second Reveal-only flex override that shrank the prompt hero and pulled the chips upward was removed. Both the outer shell and internal prompt/chip geometry now inherit the Open Question contract; the author verified the correction.
- Large Reader RTE figures no longer permit side wrapping: live diagnosis found a 700-pixel floated figure inside a 768-pixel body leaving only an unusable remainder column for prose. Large figures preserve authored alignment and push subsequent text below; smaller figure sizes retain optional wrapping. The author verified the result.
- Tile chrome follows one capability-based corner contract: upper-left owns leading selection or identity, upper-right Delete, lower-right Edit, and lower-left surface-specific status or content. Unsupported controls remain absent. Studio Cards retains Type and Status; Media retains Source, Status, and Linked; Reader uses the otherwise-unused Select corner for Type and Gallery position without gaining Select or Delete, leaving the lower image edge available for Gallery captions. Reader, Cards, and Media use the same white-pencil-on-black Edit treatment; the smaller Media pencil and Card pencil sit at the visual lower-right, and Cards and Media share the same upper-right trash symbol. Focused tests, production typecheck, and live desktop/mobile geometry verification pass.
- Reader Gallery feed behavior is restored after tracing a June 12 performance regression. Reader feed/search now use a bounded Reader tier that hydrates covers for all visible cards and the complete ordered media sequence only for Gallery cards; Administration remains cover-only and non-Gallery body media remain deferred. The published `Canada` Gallery again exposes its cover plus 12 Gallery images with `1/13`, and other visible Galleries report complete counts. Seventeen focused tests and production typecheck pass.
- Reader image chrome now uses the otherwise-unused Reader upper-left slot for Type and Gallery `x/y`. Active Gallery captions occupy the image bottom, clamp to three feed lines with full accessible/detail text, and leave clearance for lower-right Edit. The three image overlays use the existing paired strong-control theme roles; computed live colors remain blue/near-white in both Light and Dark. Twenty-nine focused tests and production typecheck pass, and the saved appearance was restored to Dark after verification.
- Explore More discovery now preserves the established Freeform model while removing one misleading case: dimensionally untagged current cards omit Related because its former request duplicated broad Unrelated sampling, while tagged cards retain shared-dimension Related and Unrelated remains available. Results are deduplicated across both generated rails and existing exclusions; child presentation and type eligibility are unchanged. Compact-rail Edit sits at the image lower-right. The 609-card read-only assessment, twenty-seven focused tests, lint, production typecheck, and live untagged/tagged Reader verification pass.
- Content Page Legibility is complete without another code change. Story titles do not overlay imagery; image badges, Gallery position, and Gallery captions use the verified paired strong-control theme roles; Edit remains white on black; and the covered Question cue owns a solid themed surface and border.
- Content Page Disclosure requires no additional implementation. Gallery Inline remains directly swipeable in the feed, Question Inline uses the completed Reveal interaction, Story is Navigate-only, and Callout is Static. The stale presentation matrix was corrected to include Static and remove the unsupported Story Inline claim. With Quote and its dependent typography explicitly parked outside the active definition, the Content Page Completion Gate passed; View Page is next.
- View Page mode continuity is repaired without mutating the saved feed preference. A valid detail-route mode now owns sidebar selection, collection-tree loading, and outgoing destinations. The live `Society & Culture` Guided parent generates five Guided Read More links and its `Movies` child remains Guided without Explore More; the live `1937 Chrysler Royale` Freeform detail generates Freeform Explore More links. Thirty-eight focused tests and production typecheck pass.
- Callout presentation is complete. Content Grid Callouts are square, show their complete bounded body and dimensional chips, and use the standard lower-right Reader Edit position. Compose blocks rendered overflow with a shorten-or-change-to-Story instruction. A Callout selected from a contextual rail opens in an expanded destination that preserves the static tile's surface, typography, spacing, and centered pushpin watermark; live Dark-theme verification confirmed all five bullets on `Culture` and its expanded detail.
- Story and Gallery detail share one media-first reading structure without merging their types. The selected cover is hero; Gallery falls back to its first authored image when no cover exists. Type kicker, Title, optional Subtitle, Tags, optional content, deduplicated remaining media, and continuation follow. Matching hero media is removed by stable ID, but global numbering, captions, caption editing, and the complete lightbox sequence remain intact. Live `Basketball Court` verification shows its cover once and Gallery images 2-6; `Scot's Birth 1961` shows hero 1/12, remaining images 2/12-12/12, and a complete 1/12 lightbox.
- Story and Gallery detail prose now has a left-aligned `68ch` desktop reading measure and explicit editorial spacing while figures retain the wider canvas and mobile uses available width. Detail-page administrator Edit uses the shared accessible white-pencil-on-black control.
- Card text-encoding repair is complete. The audit found 1,147 Unicode replacement characters across 221 of 1,281 Cards; the author reviewed and approved the 55 otherwise ambiguous characters and explicitly waived backup. A guarded migration reached zero unresolved characters before updating Firestore. Independent post-write audit reports zero affected Cards and zero replacement characters, and all 1,281 Cards were re-upserted into Typesense.
- View Page passed its Completion Gate. Guided and Freeform continuation, Story/Gallery reading structure, Gallery truth, Callout presentation, long-form measure, contextual Edit, imported-text repair, and proportional Question hierarchy are verified; Quote remains deliberately parked.
- Theme Standardization passed its Definition Gate without another architectural rewrite. The governing author cascade is Foundation → Shared system → Component → Variant, with responsive and accessibility behavior retained as fixed Guardrails. The automated inventory resolves all 109 registered Reader component attributes across 13 components and 24 variants. Foundation font inheritance, Primary action, Reader Type chip, Content Grid spacing, and Question watermark scale are governed; no inventoried macro control remains partial.
- Typography semantic ownership is assessed and enforced in the inventory: all 36 current roles have one owner, card type alone does not create a default, Story and Gallery retain intentional override slots, Question and Callout retain distinct treatments, and Quote stays parked. That assessment established the inheritance contract now implemented by the following slice.
- Foundation typography inheritance is implemented without silently migrating saved appearance. UI, Reading, and Display family sources compile through inherited role references; Journal and Editorial retain deliberate exceptions; the Foundation editor reports assigned, inherited, and overridden counts and offers explicit bulk adoption; role editors disclose inherited versus overridden family state and can return to `Use Foundation`; and legacy literal families remain overrides. Twenty-five focused compiler, inventory, and Theme-overlay tests, touched-code lint, production typecheck, and live Theme-workbench verification pass. Shared Primary action ownership is next.
- Shared Primary action ownership is implemented as one authorable background, text, border, and hover recipe for verified commit/proceed controls across Reader and Administration. Selected navigation and filters, support controls, feedback actions, Edit/Delete, resize handles, and media/lightbox controls remain separate.
- Shared Reader Type chip ownership is implemented as one authorable background, text, border, and hover recipe used by grid, rail, opened-card context, and inline Gallery chips. Base, compact, and rail sizing and placement remain proportional context guardrails.
- Content Grid spacing ownership is implemented as one authorable spacing-scale recipe used by desktop and mobile Reader grids. The prior `spacing/sm` appearance is preserved; minimum tile width, Guided sizing, mobile single-column behavior, page and rail spacing, virtualization, and overflow protection remain fixed guardrails.
- Question watermark scale ownership is implemented as one bounded 50–90% proportional treatment, paired with the existing opacity treatment across grid, Reveal, open detail, rails, and Compose preview. The prior 78% appearance is preserved; prompt fitting, optical offset, clamps, and overflow protection remain fixed guardrails.
- State and Administration standardization is implemented through explicit Administration success, error, warning, and information aliases. Admin panels, grids, forms, filters, Studio status, Theme Management, and shared app feedback no longer depend on Reader-shaped global state variables, undefined admin-warning fallbacks, or confirmed legacy state literals. Non-state brand, dimension, overlay, selection, and destructive meanings remain separate.
- Confirmed bypass and compatibility cleanup removed post-recipe Journal/Editorial alias overrides and Reader-shaped aliases from Theme Management workbench chrome. Governed dimensional and contrast variables replaced undefined fallbacks. The automated inventory records two removed bypasses, two retained delivery-safety paths, and two retained intentional paths; Reader previews, static delivery fallback, modal atomic safety, and fixed image-action contrast remain explicitly bounded.
- Workbench usability is verified without changing its hierarchy: the selected editor shows the complete scope path, dirty drafts show Unsaved changes, and Discard requires shared confirmation with Keep editing as the safe cancellation. Thirty-six focused tests, touched-code lint, and production typecheck pass. The author verified a harmless SM-to-MD spacing draft, the dirty status and confirmation, and restoration to saved SM without saving. Theme Standardization is closed.
- Preset save/switch ordering is repaired. An intermediate `Save and switch` persistence no longer refreshes the route before the requested preset is applied, so the old persisted preset cannot overwrite the new selection. Normal explicit Save still refreshes. Focused Theme tests, lint, and production typecheck pass; live verification safely activated Journal as an unsaved draft and discarded back to persisted Editorial without modifying stored data.
- The development-only Turbopack console error after Theme save is repaired at its boundary: the mutable secondary snapshot now writes atomically under ignored `.next/theme-data.backup.json` instead of rewriting the compiled root JSON module. Firestore remains primary and production retains the fallback path. Twenty-three compiler tests, lint, and production typecheck pass; confirm absence of the overlay on the next normal Theme save.
- The initial Journal audit is complete for the desktop Content Grid, sidebar/controls, and an opened Story in Light and Dark. Light's warm reading canvas works but secondary surfaces clamp to white and detach the shell; Dark contrast is numerically strong but broad brown usage flattens grid hierarchy. Opened Story presentation is the strongest shared baseline. The next gate is narrow tinted chrome/support surfaces versus a deeper global Light foundation; the narrow semantic surface is recommended.
- The follow-up Journal/Reader consistency repair deepens the Journal Light foundation so its raised Sidebar and card surface remains warm rather than white, uses the brown Type/Question accent for no-cover Question watermarks, removes detail-only outlines and washed fills from actual dimensional tags, standardizes covered Question closed/open hierarchy, separates no-cover Type and Tags, preserves the original feed/collection return across chained Explore destinations, and makes the full Reader editor Save action visible. Fifty-two focused tests pass; touched-code lint has no errors. The persisted Journal document has not yet been overwritten, so preset save plus representative mobile/desktop author review remains the active gate.
- The author accepted Journal as good enough for this phase. Editorial Light review then exposed three semantic contrast defects: Type and Gallery `x/y` differed and carried visible outlines, utility watermarks nearly disappeared, and open-Gallery arrows were near-white on a light-gray control. The implemented preset/runtime correction unifies Type and `x/y`, raises Question/Quote/Callout watermark opacity to `0.3` through their proper roles, and assigns mode-aware input text to Media Controls. Forty-nine focused tests, touched-code lint, and diff checks pass; Editorial preset save and Light/Dark author review remain required.
- Reader full edit action loss was diagnosed as horizontal min-content overflow, not a Light-theme defect: an 800-pixel Reader modal contained a roughly 1,501-pixel Compose shell, pushing Close, Discard, and Save beyond the viewport and clipping them. The Reader modal now constrains its body, shell, header, actions, and scroll region without changing the shared Studio CardForm. Automated verification passes; desktop and phone-width author confirmation remains required before closing the repair.
- Journal shell warmth is implemented as an unsaved live draft: only Journal Sidebar moves from the raised secondary surface to the warm base surface. Light now carries paper warmth through the shell; Dark lets chrome recede against cards. Editorial is unchanged. Twenty-four compiler tests, lint, and production typecheck pass, and both Reader modes were visually checked. The correct Light logo loaded after Dark-to-Light switching. Author review and Save are next.
- Journal shell warmth and chip consistency are now persisted. Author observations correctly identified dark-on-dark dimensional chips, inconsistent Gallery/Question Type treatments, and a white Type outline. Dimensional filled chips now use their dimensional recipe's light-on-fill text; covered badges no longer override the shared Type recipe; Journal Type uses principal brown with light text and matching border everywhere. The save produced no Turbopack overlay. Thirty-eight focused tests, lint, and production typecheck pass. The stale port-3003 server was restarted, and fresh SSR output confirms the saved tokens.
- Cards passed the Service Boundaries Completion Gate. Read/hydration, list/archive queries, mutation integrity, focused mutations, lifecycle/media lifecycle, bulk tags, and broad-update orchestration have explicit owners under `services/cards`; all callers migrated and the temporary `cardService.ts` facade is removed. The extraction also centralized child-placement reconciliation so both focused and broad hierarchy updates detach former parents and clear legacy root projections. Production typecheck and lint pass; 183 focused Jest tests and 23 Firestore emulator tests pass; Card content/media and subject-contract audits show no unexplained drift. A read-only media-reference audit isolated pre-existing drift to the draft `The Bond Coats of Arms` and made no changes. The final local browser-automation attempt was blocked by the browser bridge rejecting its own claimed tabs, so no new live UI result is claimed. Service Boundaries remains active with the Theme service gate next.
- Theme also passed the Service Boundaries Completion Gate. Document validation/normalization, CSS compilation/scoping, and persistence/fallback loading now have explicit owners under `services/theme`; all callers migrated and `themeService.ts` is removed. Production typecheck, lint, and build pass; three focused suites pass 166 tests, including Firestore-first save and fallback canaries. A production Reader smoke test rendered both modes with scoped CSS and no console errors; Theme Admin navigation later stalled in the browser bridge, so no interactive save result is claimed. Component Boundaries definition is next.
- Component Boundaries passed its Definition Gate in `03`. The measured seams are `StudioWorkspace` (1,548 lines), `MediaAdminContent` (1,554), `CardForm` (1,732), and Reader desktop editing's direct dependency on the full Studio form/context/CSS. Sequence Studio layout, Studio selection/actions, Media workspace, CardForm subsystems, then Reader focused editing. The first slice preserves current pane dimensions and preferences while extracting tested layout ownership.
- Component boundary implementation is substantially complete. `useStudioPaneLayout` and `studioPaneLayout` own pane state, preferences, responsive behavior, resize sessions, and layout policy; `useStudioSelectedCard` and `studioCardSelectionModel` own selected-Card loading/cache, optimistic synchronization, reconciliation, and eviction. `StudioWorkspace` is reduced from 1,548 to 736 lines. Media bulk-delete and stack operations have focused hook owners while the existing browse/exact/organize components retain presentation. Story Assist has its own CardForm subsystem, reducing CardForm from 1,732 to 1,433 lines. Reader chrome and CSS have Reader owners, and neutral `CardFormSurfaceContext` replaces Reader/CardForm dependency on Studio context; CardForm no longer imports `StudioShellContext`. Focused Studio, Media, CardForm, and Reader suites pass, as do touched-code lint and production typecheck.
- Component Boundaries passed its Completion Gate. Production typecheck, touched-code lint, diff checks, and the full production build pass; 36 focused Studio, Media, CardForm, and Reader tests pass. After the stale pre-build process on port 3000 was replaced, live browser verification confirmed pane collapse/restore, Media Browse/Exact switching and restoration, Card selection into Compose, and Reader full-editor loading with Save, Duplicate, and Close controls reachable. Closing returned to Reader detail, and Studio and Reader recorded no console errors.
- Part 3 Platform and commercial readiness passed its defined capability gate. Authorization now has one revocation-aware server policy, `/login` is the sole sign-in entry, and safe callbacks are shared. Legacy Retirement proved current redirects and `CollectionsAdminClient` are live compatibility/capability owners while prior Card/Theme facades remain retired. Search falls back truthfully to title-prefix results when Typesense is absent or unavailable and discloses degraded mode. Operations verified a complete 8,938-document/10,639-object paired backup, a zero-write disposable-target paired restore dry run, and a guarded total-admin-lockout recovery command. Engineering Quality removed invalid wildcard credentialed CORS, added baseline response protections, reduced production audit critical findings from three to zero, and passed 156 suites / 853 tests, production typecheck, lint with zero errors, and final production build output. A specific release still requires intentional commit/push and the release checklist; Next/Auth.js/Firebase major upgrades and CSP remain separately gated security maintenance.

## Workspace hygiene

- The implementation checkpoint before this handoff is commit `a75164fc` (`feat: add exact media match review queue`).
- `src/lib/services/questionService.ts` was already modified outside the Exact matches slice and must not be staged, reverted, or absorbed without separate inspection and approval.
- `.codex-dev-3003.err.log`, `.codex-dev-3003.out.log`, and `firestore-debug.log` are local untracked diagnostic files and must not be committed.
- Development browser state and port occupancy are transient. Verify them rather than relying on this handoff.
