## Recovery Status

### Already back on disk

- `docs/02-Application.md`
  - `Include sub-tags behavior`
  - `Guided discovery policy`
  - `Shared action button contract (2026-05-24)`
  - large `Studio unified shell contract` block
- `docs/03-Implementation.md`
  - `Guided mode now behaves more like a TOC`
  - `selected node's direct children (not the parent card itself)`
- `docs/04-Theme-Design-Contract.md`
  - `10.6 Addendum (2026-05-05)`
  - `Panel-anchored Studio feedback is now a real pattern`
  - `Confirmation rollout has started in Studio`

### Present in session transcript but not confirmed as current canon outcome

- A compacted rewrite of the `Administration` section in `docs/02-Application.md`
  - session evidence shows a replacement from a very long feature cluster to a shorter set beginning with:
    - `Admin Navigation`
    - `Active Domains`
    - `Studio-first Administration`
    - `Legacy Route Demotion`
    - `Shared Feedback Model`
    - `Compact Studio Shell`
    - `Operator Tooling`
    - `Integrity Gate`
    - `Studio Inline Tags (v1)`
- removal of `Guided leaf behavior` from `docs/02-Application.md`
- removal of a long generic admin-domain list from `docs/02-Application.md`

### Current local state

- `docs/02-Application.md` is not currently modified in Git.
- `docs/03-Implementation.md` is not currently modified in Git.
- `docs/04-Theme-Design-Contract.md` is currently modified in Git.
- current `docs/04-Theme-Design-Contract.md` diff is small and unrelated to the recovery addendum:
  - `` `**theme1.css`** mirrors the same names for authoring reference; `theme.css` includes fallbacks if injection is empty. ``

### Recommended next recovery target

- Compare the transcript-only compact `Administration` rewrite against the current heavy `Administration` section and decide whether to re-apply it.
