/**
 * Admin UI is intended for viewports wider than the mobile nav breakpoint.
 * Navigation hides admin links at max-width: 768px.
 * Use min-width 769px in matchMedia so behavior matches that CSS.
 */
export const ADMIN_VIEWPORT_MIN_WIDTH_PX = 769;
