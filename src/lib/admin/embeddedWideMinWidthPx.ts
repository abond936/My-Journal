/**
 * Shared min-width (px) for embedded admin “wide” layout: Collections 5-column grid and Studio
 * Media/Compose resizable row must enable/disable together.
 */
export const EMBEDDED_ADMIN_WIDE_MIN_WIDTH_PX = 981 as const;
